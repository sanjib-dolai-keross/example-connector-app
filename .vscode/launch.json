{
    "configurations": [
        {
            "type": "java",
            "name": "Spring Boot-DacExampleAppApplication<{{repo_name}}-server>",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "mainClass": "com.ikon.dacexampleapp.DacExampleAppApplication",
            "projectName": "{{repo_name}}-server",
            "args": "",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}